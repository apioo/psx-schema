struct Creature {
    kind: String,
}

struct Human {
    kind: String,
    firstName: String,
}

struct Animal {
    kind: String,
    nickname: String,
}

struct Union {
    union: Object,
    intersection: Object,
    discriminator: Object,
}
